-- Down: drop enum types for catalog module
DROP TYPE IF EXISTS uom_type CASCADE;
DROP TYPE IF EXISTS catalog_status CASCADE;
DROP TYPE IF EXISTS item_type CASCADE;
DROP TYPE IF EXISTS attribute_type CASCADE;
